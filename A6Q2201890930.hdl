CHIP A6Q2201890930 {
    IN a, b, c, d;
    OUT f;
    
    PARTS:
    Not (in=a, out=na);
    Not (in=b, out=nb);
    Not (in=c, out=nc);
    Not (in=d, out=nd);

    And (a=na, b=nb, c=nc, d=d, out=q);
    And (a=na, b=b, c=c, d=nd, out=w);
    And (a=a, b=nb, c=nc, d=d, out=e);
    And (a=a, b=nb, c=c, d=d, out=r);
    And (a=a, b=b, c=nc, d=d, out=t);

    Or (a=q, b=w, c=e, d=r, out=f1);
    Or (a=f1, b=t, out=f);

}